{
  "name": "@autofy/conference",
  "version": "1.0.0",
  "type": "module",
  "main": "./nuxt.config.ts",
  "scripts": {
    "dev": "nuxt dev",
    "build": "node -e \"const fs=require('fs');fs.mkdirSync('public/tmp',{recursive:true});fs.writeFileSync('public/tmp/restart.txt','Deployed: '+new Date().toISOString())\" && nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "static": "npx serve .output/public"
  },
  "dependencies": {
    "@autofy/base": "*"
  },
  "devDependencies": {
    "nuxt": "^4.5.0"
  }
}